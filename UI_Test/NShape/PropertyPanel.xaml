<UserControl x:Class="UI_Test.NShape.PropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI_Test.NShape"
             mc:Ignorable="d" 
             d:DesignHeight="235" d:DesignWidth="315">
    <Grid>
        <Border x:Name="panelProperty" Style="{DynamicResource LightBackground}" Width="295" Height="215" Margin="10">
            <Grid>
                <Border Style="{DynamicResource DarkBackground}" Width="275" Height="30" Margin="10,10,10,175">
                    <TextBlock FontSize="20" Foreground="White" FontWeight="Bold" Margin="10,0,0,0"><Run Language="ko-kr" Text="Properties"/></TextBlock>
                </Border>
                <Border Style="{DynamicResource DarkBackground}" Width="275" Margin="10,50,10,10"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
