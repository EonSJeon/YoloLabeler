<UserControl x:Class="UI_Test.UiConsole"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:UI_Test"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance}"
             d:DesignHeight="900" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30*"></RowDefinition>
            <RowDefinition Height="35"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"  >
            <DockPanel>
                <TextBox x:Name ="ttbox" MaxLines="3000"  PreviewMouseDown="MDown_EndFlag"
                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                         FontFamily="Consolas" FontSize="16" Background="#222222" Foreground="White"/>
            </DockPanel>
        </Grid>
        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <Button  x:Name = "BtnClear"  Content="Clear" Click="Click_BtnClear" Margin="3"   />
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
